on: [push]

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    name: Test Luamdata actions
    steps:
      - name: Lumadata yaml test
        id: hello
        uses: Lumadata/actions@v0.1.6
        with:
          repository-key: ${{ secrets.REPOSITORY_KEY }}
          repository-secret-key: ${{ secrets.REPOSITORY_SECRET_KEY }}
          path-to-directory: ./